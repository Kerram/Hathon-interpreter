def x :: Int -> Int x = x  + 19;

x 1;

def g :: Int -> Int x = g + (10 // 0);
# def g :: Int = 5 + (10 // 0);

def list :: Int -> [Int] x = x:(list x);

def fun :: Int -> Int x = x + (undeclared 2);
fun 10;

head (list 10);
